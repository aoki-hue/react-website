/* components */
import { SectionArea } from "components/utils.module";
import Information from "components/information";

const InformationPage = () => {
  return (
    <SectionArea>
      <Information informationPage />
    </SectionArea>
  );
};

export default InformationPage;
